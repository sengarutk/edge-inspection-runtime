name: "network_partitions"
description: "Extended edge broker network severance during active defect events."
total_steps: 300
machine_state_schedule:
  - { start: 0, end: 300, state: "RUNNING" }
injected_faults:
  - { fault_type: "NETWORK_PARTITION", start_step: 50, duration_steps: 60, intensity: 1.0 }
vision_defect_schedule:
  - { start: 60, end: 90 }
